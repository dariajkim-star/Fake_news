# FinFact — pinned dependencies (architecture.md Tech Stack)
# Python 3.10+

# Core DL
torch>=2.0,<3.0
torchvision>=0.15
transformers>=4.40,<5.0

# Vision branch (Epic 2+)
ultralytics>=8.0,<9.0
open_clip_torch>=2.24
insightface>=0.7
paddleocr>=2.7

# Matching / metrics
scipy>=1.10,<2.0
scikit-learn>=1.3,<2.0

# Data
numpy>=1.24,<3.0
pandas>=2.0,<3.0
Pillow>=10.0

# Config / experiment tracking
PyYAML>=6.0
wandb>=0.16
tensorboard>=2.14

# Demo (Epic 6)
gradio>=4.0,<5.0

# Testing / lint
pytest>=8.0,<9.0
black>=24.0
ruff>=0.4
